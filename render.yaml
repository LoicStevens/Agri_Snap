services:
  - type: web
    name: agrisnap-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --timeout 120
    envVars:
      - key: WEATHER_API_KEY
        value: 41164adf26a1496389710628251207
    region: oregon
    plan: free
